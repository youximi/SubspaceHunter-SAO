# Assets

Only a limited subset of project scripts is published in this repository.

The full local Unity project contains large binary files and third-party assets. Those files are intentionally excluded until their licenses are reviewed and a replacement or download strategy is defined.

